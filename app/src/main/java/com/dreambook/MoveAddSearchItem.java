package com.dreambook;

import android.view.View;
import androidx.appcompat.widget.Toolbar;

public interface MoveAddSearchItem {
        void moveAdd(Toolbar toolbar, View view);
        void delItemSearch(Toolbar toolbar, View view);
}
